name: "End-to-end tests"

on: push

defaults:
  run:
    shell: bash

jobs:
  e2e-tests:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-10.15, ubuntu-20.04]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Bootstrap
        run: . ./bootstrap.sh

      - name: Apply
        run: gulp

      - name: Re-apply
        run: gulp
