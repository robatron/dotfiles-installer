name: "Unit tests"

on: push

defaults:
  run:
    shell: bash

jobs:
  unit-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Install Node.js dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test -- --ci
