name: "Unit tests"

on: push

defaults:
  run:
    shell: bash

jobs:
  unit-tests:
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Install Node.js dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test -- --ci
